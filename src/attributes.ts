type AttributeLayout = {
    location: number;
    size: number;
    type: number;
    normalized: boolean;
    stride: number;
    offset: number;
    divisor?: number;
};

export const ATTRIBUTES_LAYOUT: AttributeLayout[] = [

];
